export function Footer() {
  // TODO: bring back a venue/RSVP section (see src/data/config.ts for the
  // placeholder fields) once those details are finalized.
  return (
    <footer className="footer">
      <p className="footer__signoff">Can't wait to celebrate with you.</p>
    </footer>
  );
}
